export function BoardPage() {
  return (
    <div>
      <h1 className="text-xl font-semibold tracking-tight">Board</h1>
      <p className="mt-2 text-sm text-muted">
        The Kanban board (columns, goals, drag &amp; drop) arrives in an upcoming step.
      </p>
    </div>
  )
}
